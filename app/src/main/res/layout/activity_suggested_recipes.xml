<?xml version="1.0" encoding="utf-8"?>
<!--
    The Suggested Recipes screen.

    Two lists on one screen: recipes that can be cooked now, and recipes missing
    exactly one ingredient. They are kept visually separate with their own headings
    so the strict suggestions are never confused with the "almost there" ones.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background"
    tools:context=".ui.SuggestedRecipesActivity">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/green_primary"
        app:title="@string/title_suggested"
        app:titleTextColor="@color/white"
        app:navigationIconTint="@color/white" />

    <!-- The whole page scrolls as one, rather than each list scrolling separately. -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="24dp">

            <!-- Shown only when the pantry is empty or nothing matches. -->
            <TextView
                android:id="@+id/textEmptyState"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="32dp"
                android:textAlignment="center"
                android:textSize="15sp"
                android:textColor="@color/text_secondary"
                android:visibility="gone"
                tools:text="No recipes match your pantry yet."
                tools:visibility="visible" />

            <TextView
                android:id="@+id/headerMakeable"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingHorizontal="16dp"
                android:paddingTop="16dp"
                android:paddingBottom="4dp"
                android:text="@string/header_makeable"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/green_dark" />

            <TextView
                android:id="@+id/textSummary"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingHorizontal="16dp"
                android:paddingBottom="8dp"
                android:textSize="13sp"
                android:textColor="@color/text_secondary"
                tools:text="3 of 18 recipes" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerMakeable"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:nestedScrollingEnabled="false"
                tools:listitem="@layout/item_recipe"
                tools:itemCount="2" />

            <!-- The bonus list. Hidden entirely when nothing is one ingredient away. -->
            <LinearLayout
                android:id="@+id/sectionAlmost"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone"
                tools:visibility="visible">

                <View
                    android:layout_width="match_parent"
                    android:layout_height="1dp"
                    android:layout_marginHorizontal="16dp"
                    android:layout_marginTop="20dp"
                    android:background="@color/divider" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingHorizontal="16dp"
                    android:paddingTop="16dp"
                    android:text="@string/header_almost"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@color/expiring_soon" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingHorizontal="16dp"
                    android:paddingBottom="8dp"
                    android:text="@string/header_almost_subtitle"
                    android:textSize="13sp"
                    android:textColor="@color/text_secondary" />

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerAlmost"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:nestedScrollingEnabled="false"
                    tools:listitem="@layout/item_recipe"
                    tools:itemCount="2" />

            </LinearLayout>

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

    <include layout="@layout/view_bottom_nav" />

</LinearLayout>
