<?xml version="1.0" encoding="utf-8"?>
<!--
    The layout for ONE row in the pantry list.

    This file describes a single item only. The RecyclerView creates as many copies of
    this layout as it needs to fill the screen, and the adapter fills each copy with a
    different pantry item's data. That is why nothing here contains real text — the
    values shown are placeholders visible only in Android Studio's preview.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp">

        <!--
            layout_weight="1" makes this column expand to fill whatever horizontal
            space is left over after the quantity on the right has taken what it
            needs. Without it, a long ingredient name would push the quantity off the
            edge of the screen.
        -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/textName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="17sp"
                android:textStyle="bold"
                android:textColor="@color/text_primary"
                tools:text="Tomatoes" />

            <TextView
                android:id="@+id/textExpiry"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textSize="13sp"
                android:textColor="@color/text_secondary"
                tools:text="Expires 2026-08-20" />

        </LinearLayout>

        <TextView
            android:id="@+id/textQuantity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:background="@color/green_light"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/green_dark"
            tools:text="500 g" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
