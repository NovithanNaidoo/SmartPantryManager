<?xml version="1.0" encoding="utf-8"?>
<!--
    Toolbar menu for the pantry list.

    Temporary navigation so the other screens can be reached and tested. A bottom
    navigation bar replaces this in step 12.
-->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_suggested"
        android:title="@string/action_suggested"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_all_recipes"
        android:title="@string/action_all_recipes"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_settings"
        android:title="@string/action_settings"
        app:showAsAction="never" />

</menu>
