<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        Android automatically uses this file instead of values/themes.xml when the
        phone is in dark mode.

        It deliberately points at the same light theme, so the app looks identical
        whether or not the user has dark mode switched on. Without this file the
        dark theme would take over and the hardcoded light colours in the layouts
        would clash with it - light text on white cards.
    -->
    <style name="Base.Theme.SmartPantryManager" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/green_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:statusBarColor">@color/green_dark</item>
    </style>

    <style name="Theme.SmartPantryManager" parent="Base.Theme.SmartPantryManager" />
</resources>
